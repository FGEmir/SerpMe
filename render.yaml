services:
  - type: web
    name: pazar-pusulasi
    runtime: docker
    healthCheckPath: /healthz
    envVars:
      - key: SERPAPI_KEY
        sync: false
      - key: MAX_SERPAPI_CALLS_PER_HOUR
        value: "30"
